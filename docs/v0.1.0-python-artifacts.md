# v0.1.0 Python 候选产物记录

这份记录对应 issue #3 的 Python 3.12 CLI 与 sidecar 候选构建。二进制文件本身不进入 Git；本页只保存可审查的构建来源、验证结果和文件指纹。

## 构建来源

- 构建日期：2026-09-19
- 源提交：`87b12761ee77719df6a50067533196f8632a4855`
- 构建系统：Windows 11 x64，Build 26200
- Python：3.12.10 x64
- PyInstaller：6.22.3
- CLI 脚本：`scripts/build-cli.ps1`
- sidecar 脚本：`scripts/build-sidecar.ps1`

构建在指向上述提交的干净 worktree 中完成。构建命令显式传入 Python 3.12 虚拟环境解释器；两个仓库脚本均通过自身的 Python 版本检查。

## 文件指纹

| 产物 | 文件大小 | SHA-256 |
| --- | ---: | --- |
| `release/qingwu.exe` | 24,356,327 bytes | `4d1a4bd98459dc37a0d737c1b0dfef38bab537fc5a34fbaf458676fa63f125a1` |
| `build/sidecar/qingwu-sidecar.exe` | 24,352,756 bytes | `b17938d6656bd467aaf30e488fcb47dc536429acc3de9237122b436976c365ac` |
| `src-tauri/binaries/qingwu-sidecar-x86_64-pc-windows-msvc.exe` | 24,352,756 bytes | `b17938d6656bd467aaf30e488fcb47dc536429acc3de9237122b436976c365ac` |

Tauri 命名的 sidecar 副本与原始 sidecar 构建输出大小和 SHA-256 完全一致。

## 验证结果

```text
python -m pytest
23 passed

qingwu.exe --version
qingwu 0.1.0

qingwu.exe template validate templates/activity-organization.yaml
valid: activity-organization 1.0.0

qingwu.exe template validate templates/material-collection.yaml
valid: material-collection 1.0.0

qingwu.exe template validate templates/reimbursement.yaml
valid: reimbursement 1.0.0

qingwu-sidecar.exe
template.list 成功返回三个内置模板
```

产物复制到仓库约定的忽略目录后，重新运行了 CLI `--version`、sidecar `template.list` 和 SHA-256 校验，结果保持一致。

## Git 边界

以下路径由 `.gitignore` 排除：

- `release/`
- `build/`
- `src-tauri/binaries/qingwu-sidecar*.exe`

构建后执行 `git ls-files "*.exe"` 无输出，因此没有 EXE 进入 Git 历史。

## 尚未覆盖

本记录不代表 Tauri 安装包已经完成，也不替代无 Python 开发环境的干净 Windows 安装测试。MSI、NSIS、代码签名、安装和卸载验收由后续桌面生产打包任务负责。
